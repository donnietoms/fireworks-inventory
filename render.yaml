services:
  - type: web
    name: fireworks-inventory
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.js & npx serve -s dist -l $PORT
    envVars:
      - key: NODE_VERSION
        value: 20
